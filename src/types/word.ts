export type TWordItem = {
  detail: string;
  name: string;
}
export type TWordArray = Array<TWordItem>

export type TWordObject = {
  detail: string;
  name: string;
  length: number;
  chars: string[];
}
