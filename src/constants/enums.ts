export enum Routes {
  Home = "/",
  Api = "/api",
};

export enum Events {
  HistoryChanged = "HistoryChanged"
};

export enum EThemes {
  Dark = "Dark",
  Light = "Light"
}

export enum ELocalStorage {
  Theme = "crossword/ts-theme",
}